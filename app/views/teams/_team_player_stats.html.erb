<!-- OVERWIEV -->

<div>
  <table id="over_p" class="sts_table pl_table">
    <tr>
      <th>Player</th>
      <th>Team</th>
      <th><abbr title="Position">P</abbr></th>
      <th><abbr title="Points">PTS</abbr></th>
      <th><abbr title="Minutes Played">MP</abbr></th>
    </tr>

    <% players.order(total_points: :desc, minutes: :desc).each do |player| %>
      <tr>
        <td class="text-left"><%= link_to player.full_name, player %></td>
        <td class="text-left"><%= link_to player.team.team_name, team_path(player.team_id) %></td>
        <% case player.position
          when 1 %><td class="text-left">GK</td>
          <% when 2 %><td class="text-left">DEF</td>
          <% when 3 %><td class="text-left">MID</td>
          <% when 4 %><td class="text-left">FWD</td>
        <% end %>
        <td><%= player.total_points %></td>
        <td><%= player.minutes %></td>
      </tr>
    <% end %>
  </table>
</div>

<!-- OFFENSIVE -->

<div>
  <table id="offe_p" class="sts_table pl_table collapse">
    <tr>
      <th>Player</th>
      <th>Team</th>
      <th><abbr title="Position">P</abbr></th>
      <th><abbr title="Goals">G</abbr></th>
      <th><abbr title="Assists">A</abbr></th>
    </tr>

    <% players.order(goals_scored: :desc, assists: :desc).each do |player| %>
      <tr>
        <td class="text-left"><%= link_to player.full_name, player %></td>
        <td class="text-left"><%= link_to player.team.team_name, team_path(player.team_id) %></td>
        <% case player.position
          when 1 %><td class="text-left">GK</td>
          <% when 2 %><td class="text-left">DEF</td>
          <% when 3 %><td class="text-left">MID</td>
          <% when 4 %><td class="text-left">FWD</td>
        <% end %>
        <td><%= player.goals_scored %></td>
        <td><%= player.assists %></td>
      </tr>
    <% end %>
  </table>
</div>

<!-- DEFFENSIVE -->

<div>
  <table id="deff_p" class="sts_table pl_table collapse">
    <tr>
      <th>Player</th>
      <th>Team</th>
      <th><abbr title="Position">P</abbr></th>
      <th><abbr title="Clean Sheets">CS</abbr></th>
      <th><abbr title="Tackles">T</abbr></th>
    </tr>

    <% players.order(clean_sheets: :desc, tackles: :desc).each do |player| %>
      <tr>
        <td class="text-left"><%= link_to player.full_name, player %></td>
        <td class="text-left"><%= link_to player.team.team_name, team_path(player.team_id) %></td>
        <% case player.position
          when 1 %><td class="text-left">GK</td>
          <% when 2 %><td class="text-left">DEF</td>
          <% when 3 %><td class="text-left">MID</td>
          <% when 4 %><td class="text-left">FWD</td>
        <% end %>
        <td><%= player.clean_sheets %></td>
        <td><%= player.tackles %></td>
      </tr>
    <% end %>
  </table>
</div>

<!-- BOOKING -->

<div>
  <table id="book_p" class="sts_table pl_table collapse">
    <tr>
      <th>Player</th>
      <th>Team</th>
      <th><abbr title="Position">P</abbr></th>
      <th><abbr title="Yellow Cards">YC</abbr></th>
      <th><abbr title="Red Cards">RC</abbr></th>
    </tr>

    <% players.order(red_cards: :desc, yellow_cards: :desc).each do |player| %>
      <tr>
        <td class="text-left"><%= link_to player.full_name, player %></td>
        <td class="text-left"><%= link_to player.team.team_name, team_path(player.team_id) %></td>
        <% case player.position
          when 1 %><td class="text-left">GK</td>
          <% when 2 %><td class="text-left">DEF</td>
          <% when 3 %><td class="text-left">MID</td>
          <% when 4 %><td class="text-left">FWD</td>
        <% end %>
        <td><%= player.yellow_cards %></td>
        <td><%= player.red_cards %></td>
      </tr>
    <% end %>
  </table>
</div>
