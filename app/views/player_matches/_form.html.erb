<%= form_with(model: player_match, local: true) do |form| %>
  <% if player_match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player_match.errors.count, "error") %> prohibited this player_match from being saved:</h2>

      <ul>
      <% player_match.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
